# Example .env file for CI/CD and local development
# Fill in real values locally, but keep this file safe for public use
DOMAIN=localhost
SMTP_HOST=
TRAEFIK_PUBLIC_NETWORK=traefik-public
INSTALL_DEV=true
# Add other variables as needed
